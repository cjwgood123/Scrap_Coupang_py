# 🎯 실행 위치 및 방법

## 📍 어디서 실행하나요?

**프로젝트 폴더에서 실행하세요!**

```
C:\JProject\coupang_batch
```

이 폴더에 다음 파일들이 있어야 합니다:
- `coupang_scraper_final.py` (실행할 파일)
- `venv` (가상환경 폴더)
- `requirements.txt`

---

## 🚀 실행 방법

### 1단계: 프로젝트 폴더로 이동

**PowerShell에서:**
```powershell
cd C:\JProject\coupang_batch
```

**Git Bash에서:**
```bash
cd /c/JProject/coupang_batch
```

**명령 프롬프트(CMD)에서:**
```cmd
cd C:\JProject\coupang_batch
```

### 2단계: 가상환경 활성화

**PowerShell:**
```powershell
.\venv\Scripts\Activate.ps1
```

**Git Bash:**
```bash
source venv/Scripts/activate
```

**CMD:**
```cmd
venv\Scripts\activate.bat
```

### 3단계: 실행

```bash
python coupang_scraper_final.py
```

---

## ✅ 확인 방법

프롬프트 앞에 `(venv)`가 보이면 가상환경이 활성화된 것입니다:

```
(venv) PS C:\JProject\coupang_batch>
```

또는

```
(venv) user@GN-cjwgood123 MINGW64 /c/JProject/coupang_batch
```

---

## 💡 빠른 실행 (한 번에)

**PowerShell:**
```powershell
cd C:\JProject\coupang_batch
.\venv\Scripts\Activate.ps1
python coupang_scraper_final.py
```

**Git Bash:**
```bash
cd /c/JProject/coupang_batch
source venv/Scripts/activate
python coupang_scraper_final.py
```

---

## 📂 현재 위치 확인

현재 어느 폴더에 있는지 확인하려면:

**PowerShell/CMD:**
```powershell
pwd
# 또는
cd
```

**Git Bash:**
```bash
pwd
```

---

## ⚠️ 주의사항

- 반드시 `C:\JProject\coupang_batch` 폴더에서 실행해야 합니다!
- 다른 폴더에서 실행하면 파일을 찾을 수 없다는 오류가 발생합니다.
- 가상환경(`venv`)이 활성화되어 있어야 합니다.

