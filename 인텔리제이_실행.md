# 🚀 IntelliJ IDEA Git Bash에서 실행하기

## 📍 IntelliJ IDEA Git Bash 터미널에서

### 1단계: 가상환경 활성화

```bash
source venv/Scripts/activate
```

### 2단계: 실행

```bash
python coupang_scraper_final.py
```

---

## ✅ 전체 명령어 (한 번에 복사)

```bash
source venv/Scripts/activate
python coupang_scraper_final.py
```

---

## 💡 확인 방법

가상환경이 활성화되면 프롬프트 앞에 `(venv)`가 표시됩니다:

```
(venv) user@GN-cjwgood123 MINGW64 /c/JProject/coupang_batch
```

---

## ⚠️ 주의사항

- IntelliJ IDEA의 Git Bash 터미널은 이미 프로젝트 폴더(`C:\JProject\coupang_batch`)에 있을 것입니다.
- 가상환경이 활성화되지 않으면 패키지를 찾을 수 없다는 오류가 발생합니다.
- `(venv)`가 보이지 않으면 가상환경 활성화를 다시 해주세요.

---

## 🔄 다음번 실행 시

가상환경만 활성화하고 실행하면 됩니다:

```bash
source venv/Scripts/activate
python coupang_scraper_final.py
```

